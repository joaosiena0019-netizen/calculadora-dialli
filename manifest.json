<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no">

<title>Calculadora Dialli</title>

<meta name="theme-color" content="#06351f">
<meta name="mobile-web-app-capable" content="yes">
<meta name="apple-mobile-web-app-capable" content="yes">
<meta name="apple-mobile-web-app-title" content="Calculadora Dialli">
<meta name="apple-touch-fullscreen" content="yes">

<link rel="manifest" href="./manifest.json">
<link rel="icon" type="image/png" href="./icon-180.png">
<link rel="apple-touch-icon" href="./icon-180.png">

<style>
*{
    box-sizing:border-box;
    -webkit-tap-highlight-color:transparent;
}

html,body{
    margin:0;
    padding:0;
    min-height:100%;
    font-family:Arial,Helvetica,sans-serif;
    background:#06351f;
}

body{
    color:#fff;
}

.app{
    min-height:100vh;
    padding:8px 10px 25px;
    background:linear-gradient(145deg,#012c18,#00652f,#01351d);
}

.container{
    width:100%;
    max-width:850px;
    margin:auto;
}

.tabs{
    display:flex;
    justify-content:center;
    gap:6px;
    margin-bottom:6px;
}

.tabs button{
    border:1px solid rgba(255,255,255,.5);
    background:rgba(255,255,255,.12);
    color:white;
    border-radius:12px;
    padding:8px 20px;
    font-size:13px;
    font-weight:bold;
}

.tabs button.active{
    background:#ffd000;
    color:#06351f;
}

.logo{
    width:min(95%,620px);
    margin:auto;
    text-align:center;
}

.logo img{
    width:100%;
    height:auto;
    display:block;
}

.title{
    text-align:center;
    margin:0 0 12px;
}

.title h1{
    margin:0;
    font-size:30px;
    font-weight:900;
}

.title h1 span{
    color:#ffd000;
}

.title p{
    margin:4px 0 0;
    font-size:14px;
}

.screen{
    display:none;
}

.screen.active{
    display:block;
}

/* MARGEM */

.margin-card{
    background:#fff;
    color:#123;
    border-radius:24px;
    padding:16px;
    border:3px solid #15914b;
}

.field{
    margin-bottom:14px;
}

.field label{
    display:block;
    margin-bottom:6px;
    color:#075b2c;
    font-size:15px;
    font-weight:bold;
}

.input-box{
    display:flex;
    height:54px;
    border:2px solid #15914b;
    border-radius:13px;
    overflow:hidden;
    background:white;
}

.input-box span{
    display:flex;
    align-items:center;
    padding:0 14px;
    background:#075b2c;
    color:white;
    font-weight:bold;
}

.input-box input{
    width:100%;
    border:0;
    outline:0;
    font-size:20px;
    padding:0 12px;
}

.results{
    display:grid;
    grid-template-columns:1fr 1fr;
    gap:7px;
}

.result{
    background:#f7faf8;
    border-radius:16px;
    padding:12px 8px;
    text-align:center;
    border:1px solid #d8e6dc;
}

.result h3{
    margin:0;
    color:#138a46;
    font-size:13px;
}

.result .value{
    margin-top:5px;
    color:#075b2c;
    font-size:22px;
    font-weight:900;
}

.result p{
    margin:3px 0 0;
    color:#68756f;
    font-size:11px;
}

.formulas{
    margin-top:10px;
    padding:12px;
    border-radius:15px;
    background:#eef8f0;
    color:#194c2d;
    text-align:center;
    font-size:12px;
    line-height:1.6;
}

.formulas strong{
    color:#075b2c;
    font-size:14px;
}

.clear-margin{
    width:100%;
    height:50px;
    margin-top:10px;
    border:0;
    border-radius:14px;
    background:#075b2c;
    color:white;
    font-size:15px;
    font-weight:bold;
}

/* CALCULADORA */

.calc-card{
    background:white;
    border-radius:24px;
    padding:12px;
    border:3px solid #15914b;
}

.memory{
    display:grid;
    grid-template-columns:repeat(5,1fr);
}

.memory button{
    height:32px;
    border:0;
    background:transparent;
    color:#075b2c;
    font-size:12px;
    font-weight:bold;
}

.display{
    min-height:130px;
    padding:10px 15px;
    background:#06351f;
    border-radius:18px;
    display:flex;
    flex-direction:column;
    justify-content:flex-end;
    text-align:right;
    overflow:hidden;
}

.expression{
    min-height:34px;
    color:#aebbb4;
    font-size:clamp(17px,4vw,25px);
    white-space:nowrap;
    overflow:hidden;
    text-overflow:ellipsis;
}

.result-display{
    color:white;
    font-size:clamp(42px,11vw,64px);
    line-height:1.05;
    font-weight:900;
    white-space:nowrap;
    overflow:hidden;
    text-overflow:ellipsis;
}

.keys{
    display:grid;
    grid-template-columns:repeat(4,1fr);
    gap:2px;
    margin-top:2px;
}

.keys button{
    height:65px;
    border:0;
    border-radius:50%;
    background:transparent;
    color:#075b2c;
    font-size:26px;
    font-weight:500;
}

.keys button:active{
    background:rgba(21,145,75,.13);
    transform:scale(.92);
}

.keys .operator{
    color:#15914b;
    font-size:30px;
}

.keys .clear{
    color:#df423c;
}

.keys .equal{
    width:78px;
    height:78px;
    justify-self:center;
    align-self:center;
    border-radius:50%;
    background:#21c94b;
    color:white;
    font-size:35px;
    font-weight:bold;
}

.install{
    width:100%;
    height:53px;
    margin-top:12px;
    border:0;
    border-radius:15px;
    background:#15914b;
    color:white;
    font-size:15px;
    font-weight:bold;
}

.footer{
    text-align:center;
    padding:18px 0 0;
    font-size:14px;
}

.footer strong{
    display:block;
    margin-top:2px;
    font-size:21px;
}

@media(max-width:600px){

    .app{
        padding:6px 8px 20px;
    }

    .logo{
        width:100%;
    }

    .title h1{
        font-size:27px;
    }

    .title p{
        font-size:13px;
    }

    .keys button{
        height:60px;
        font-size:24px;
    }

    .keys .equal{
        width:72px;
        height:72px;
        font-size:32px;
    }
}
</style>
</head>

<body>

<div class="app">

<div class="container">

<div class="tabs">
<button id="marginTab" class="active">MARGEM</button>
<button id="calcTab">CALCULADORA</button>
</div>

<!-- LOGO ORIGINAL DA DIALLI DENTRO DO APP -->
<div class="logo">
<img src="./logo-dialli.png" alt="Dialli Distribuidora">
</div>

<div class="title">
<h1>CALCULADORA <span>DIALLI</span></h1>
<p>Calculadora completa para contas rápidas do dia a dia.</p>
</div>

<!-- MARGEM -->

<section id="marginScreen" class="screen active">

<div class="margin-card">

<div class="field">

<label>CUSTO DO PRODUTO</label>

<div class="input-box">
<span>R$</span>
<input id="cost" inputmode="decimal" autocomplete="off" placeholder="Digite o custo">
</div>

</div>

<div class="field">

<label>PREÇO DE VENDA / OFERTA</label>

<div class="input-box">
<span>R$</span>
<input id="sale" inputmode="decimal" autocomplete="off" placeholder="Digite o preço">
</div>

</div>

<div class="results">

<div class="result">
<h3>PREÇO DE VENDA</h3>
<div id="saleResult" class="value">R$ 0,00</div>
<p>Preço informado</p>
</div>

<div class="result">
<h3>LUCRO BRUTO</h3>
<div id="profitResult" class="value">R$ 0,00</div>
<p>Venda − custo</p>
</div>

<div class="result">
<h3>MARGEM REAL</h3>
<div id="marginResult" class="value">0,00%</div>
<p>Lucro ÷ venda</p>
</div>

<div class="result">
<h3>MARKUP</h3>
<div id="markupResult" class="value">0,00x</div>
<p>Venda ÷ custo</p>
</div>

</div>

<div class="formulas">
<strong>FÓRMULAS</strong><br>
Lucro = Preço de Venda − Custo<br>
Margem = Lucro ÷ Preço de Venda × 100<br>
Markup = Preço de Venda ÷ Custo
</div>

<button id="clearMargin" class="clear-margin">
↻ &nbsp; LIMPAR TUDO
</button>

</div>

</section>

<!-- CALCULADORA -->

<section id="calcScreen" class="screen">

<div class="calc-card">

<div class="memory">
<button data-memory="MC">MC</button>
<button data-memory="MR">MR</button>
<button data-memory="M+">M+</button>
<button data-memory="M-">M−</button>
<button data-memory="MS">MS</button>
</div>

<div class="display">

<div id="expression" class="expression"></div>

<div id="display" class="result-display">0</div>

</div>

<div class="keys">

<button class="clear" data-action="clear">C</button>
<button data-action="parentheses">( )</button>
<button data-action="percent">%</button>
<button class="operator" data-operator="/">÷</button>

<button data-number="7">7</button>
<button data-number="8">8</button>
<button data-number="9">9</button>
<button class="operator" data-operator="*">×</button>

<button data-number="4">4</button>
<button data-number="5">5</button>
<button data-number="6">6</button>
<button class="operator" data-operator="-">−</button>

<button data-number="1">1</button>
<button data-number="2">2</button>
<button data-number="3">3</button>
<button class="operator" data-operator="+">+</button>

<button data-action="sign">+/−</button>
<button data-number="0">0</button>
<button data-action="decimal">,</button>
<button class="equal" data-action="equals">=</button>

</div>

</div>

</section>

<button id="installButton" class="install">
📲 &nbsp; INSTALAR CALCULADORA DIALLI
</button>

<div class="footer">
criado e desenvolvido por
<strong>João Siena.</strong>
</div>

</div>
</div>

<script>

let tokens=[];
let current="0";
let entering=false;
let lastExpression="";
let memory=0;

const display=document.getElementById("display");
const expression=document.getElementById("expression");

const numberFormat=new Intl.NumberFormat("pt-BR",{
    useGrouping:true,
    maximumFractionDigits:12
});

function formatNumber(value){

    const n=Number(value);

    if(!Number.isFinite(n)) return "Erro";

    return numberFormat.format(n);
}

function render(){

    display.textContent=current==="Erro"
        ? "Erro"
        : formatNumber(current);

    if(lastExpression){
        expression.textContent=lastExpression;
        return;
    }

    let text="";

    for(const token of tokens){

        if(token==="+") text+=" + ";
        else if(token==="-") text+=" − ";
        else if(token==="*") text+=" × ";
        else if(token==="/") text+=" ÷ ";
        else text+=token;

    }

    if(!entering && current!=="0"){
        text+=(tokens.length ? " " : "")+formatNumber(current);
    }

    expression.textContent=text.trim();
}

function clearCalculator(){

    tokens=[];
    current="0";
    entering=false;
    lastExpression="";
    render();

}

function addNumber(number){

    lastExpression="";

    if(current==="Erro"){
        clearCalculator();
    }

    if(entering){

        current=String(number);
        entering=false;

    }else{

        if(current==="0"){
            current=String(number);
        }else{
            current+=String(number);
        }

    }

    render();

}

function addDecimal(){

    lastExpression="";

    if(current==="Erro"){
        clearCalculator();
    }

    if(entering){

        current="0.";
        entering=false;

    }else if(!current.includes(".")){

        current+=".";

    }

    render();

}

function addOperator(operator){

    if(current==="Erro") return;

    lastExpression="";

    if(!entering){

        tokens.push(current);
        entering=true;

    }

    if(
        tokens.length &&
        ["+","-","*","/"].includes(tokens[tokens.length-1])
    ){

        tokens[tokens.length-1]=operator;

    }else{

        tokens.push(operator);

    }

    render();

}

function evaluateExpression(input){

    const values=[];
    const operators=[];

    function priority(op){

        if(op==="*" || op==="/") return 2;
        if(op==="+" || op==="-") return 1;

        return 0;

    }

    function apply(){

        if(values.length<2) return false;

        const b=Number(values.pop());
        const a=Number(values.pop());
        const op=operators.pop();

        let result;

        if(op==="+") result=a+b;
        else if(op==="-") result=a-b;
        else if(op==="*") result=a*b;
        else if(op==="/"){

            if(b===0) return false;

            result=a/b;

        }else return false;

        values.push(result);

        return true;

    }

    for(let i=0;i<input.length;i++){

        const token=input[i];

        if(token==="("){

            operators.push(token);
            continue;

        }

        if(token===")"){

            while(
                operators.length &&
                operators[operators.length-1]!=="("
            ){

                if(!apply()) return null;

            }

            if(!operators.length) return null;

            operators.pop();

            continue;

        }

        if(["+","-","*","/"].includes(token)){

            if(
                token==="-" &&
                (
                    i===0 ||
                    input[i-1]==="(" ||
                    ["+","-","*","/"].includes(input[i-1])
                )
            ){

                values.push(0);

            }

            while(
                operators.length &&
                operators[operators.length-1]!=="(" &&
                priority(operators[operators.length-1])>=priority(token)
            ){

                if(!apply()) return null;

            }

            operators.push(token);
            continue;

        }

        const n=Number(token);

        if(!Number.isFinite(n)) return null;

        values.push(n);

    }

    while(operators.length){

        if(operators[operators.length-1]==="(") return null;

        if(!apply()) return null;

    }

    if(values.length!==1) return null;

    return values[0];

}

function calculate(){

    if(current==="Erro") return;

    let list=tokens.slice();

    if(!entering){
        list.push(current);
    }

    while(
        list.length &&
        ["+","-","*","/"].includes(list[list.length-1])
    ){
        list.pop();
    }

    if(!list.length) return;

    const result=evaluateExpression(list);

    lastExpression=
        list.map(token=>{

            if(token==="*") return "×";
            if(token==="/") return "÷";
            if(token==="-") return "−";

            return isNaN(Number(token))
                ? token
                : formatNumber(token);

        }).join(" ")+" =";

    if(
        result===null ||
        !Number.isFinite(result)
    ){

        current="Erro";

    }else{

        current=String(
            Math.round(
                (result+Number.EPSILON)*1000000000000
            )/1000000000000
        );

    }

    tokens=[];
    entering=true;

    render();

}

function percentage(){

    if(current==="Erro") return;

    let value=Number(current);

    if(!Number.isFinite(value)) return;

    if(tokens.length){

        const op=tokens[tokens.length-1];

        if(op==="+" || op==="-"){

            for(let i=tokens.length-2;i>=0;i--){

                if(!isNaN(Number(tokens[i]))){

                    value=Number(tokens[i])*value/100;
                    break;

                }

            }

        }else{

            value=value/100;

        }

    }else{

        value=value/100;

    }

    current=String(value);
    entering=false;

    render();

}

function changeSign(){

    if(
        current==="Erro" ||
        current==="0"
    ) return;

    current=current.startsWith("-")
        ? current.substring(1)
        : "-"+current;

    render();

}

function parentheses(){

    lastExpression="";

    const open=tokens.filter(x=>x==="(").length;
    const close=tokens.filter(x=>x===")").length;

    if(!entering){

        tokens.push(current);
        current="0";
        entering=true;

    }

    if(open>close){

        tokens.push(")");

    }else{

        tokens.push("*");
        tokens.push("(");

    }

    render();

}

document.querySelectorAll("[data-number]").forEach(button=>{

    button.onclick=()=>{
        addNumber(button.dataset.number);
    };

});

document.querySelectorAll("[data-operator]").forEach(button=>{

    button.onclick=()=>{
        addOperator(button.dataset.operator);
    };

});

document.querySelectorAll("[data-action]").forEach(button=>{

    button.onclick=()=>{

        const action=button.dataset.action;

        if(action==="clear") clearCalculator();
        if(action==="decimal") addDecimal();
        if(action==="equals") calculate();
        if(action==="percent") percentage();
        if(action==="sign") changeSign();
        if(action==="parentheses") parentheses();

    };

});

document.querySelectorAll("[data-memory]").forEach(button=>{

    button.onclick=()=>{

        const action=button.dataset.memory;
        const value=Number(current);

        if(action==="MC") memory=0;

        if(action==="MR"){

            current=String(memory);
            entering=false;

        }

        if(action==="MS" && Number.isFinite(value)){
            memory=value;
        }

        if(action==="M+" && Number.isFinite(value)){
            memory+=value;
        }

        if(action==="M-" && Number.isFinite(value)){
            memory-=value;
        }

        render();

    };

});

document.addEventListener("keydown",event=>{

    if(event.key>="0" && event.key<="9"){
        addNumber(event.key);
    }

    else if(["+","-","*","/"].includes(event.key)){
        addOperator(event.key);
    }

    else if(event.key==="." || event.key===","){
        addDecimal();
    }

    else if(event.key==="Enter" || event.key==="="){
        event.preventDefault();
        calculate();
    }

    else if(event.key==="Escape" || event.key==="Delete"){
        clearCalculator();
    }

    else if(event.key==="%"){
        percentage();
    }

});

/* ABAS */

const marginTab=document.getElementById("marginTab");
const calcTab=document.getElementById("calcTab");

const marginScreen=document.getElementById("marginScreen");
const calcScreen=document.getElementById("calcScreen");

marginTab.onclick=()=>{

    marginTab.classList.add("active");
    calcTab.classList.remove("active");

    marginScreen.classList.add("active");
    calcScreen.classList.remove("active");

};

calcTab.onclick=()=>{

    calcTab.classList.add("active");
    marginTab.classList.remove("active");

    calcScreen.classList.add("active");
    marginScreen.classList.remove("active");

};

/* MARGEM */

const costInput=document.getElementById("cost");
const saleInput=document.getElementById("sale");

function parseBrazilianNumber(value){

    value=String(value||"")
        .trim()
        .replace(/\s/g,"");

    if(!value) return null;

    if(value.includes(",")){

        value=value
            .replace(/\./g,"")
            .replace(",",".");
            
    }else if(value.includes(".")){

        const parts=value.split(".");

        if(parts.length>2){

            value=value.replace(/\./g,"");

        }else if(
            parts.length===2 &&
            parts[1].length===3
        ){

            value=value.replace(".","");

        }

    }

    const number=Number(value);

    return Number.isFinite(number)
        ? number
        : null;

}

const moneyFormat=new Intl.NumberFormat("pt-BR",{
    style:"currency",
    currency:"BRL"
});

function calculateMargin(){

    const cost=parseBrazilianNumber(costInput.value);
    const sale=parseBrazilianNumber(saleInput.value);

    if(cost===null || sale===null || cost<=0){

        document.getElementById("saleResult").textContent="R$ 0,00";
        document.getElementById("profitResult").textContent="R$ 0,00";
        document.getElementById("marginResult").textContent="0,00%";
        document.getElementById("markupResult").textContent="0,00x";

        return;

    }

    const profit=sale-cost;
    const margin=sale!==0 ? (profit/sale)*100 : 0;
    const markup=sale/cost;

    document.getElementById("saleResult").textContent=
        moneyFormat.format(sale);

    document.getElementById("profitResult").textContent=
        moneyFormat.format(profit);

    document.getElementById("marginResult").textContent=
        margin.toLocaleString("pt-BR",{
            minimumFractionDigits:2,
            maximumFractionDigits:2
        })+"%";

    document.getElementById("markupResult").textContent=
        markup.toLocaleString("pt-BR",{
            minimumFractionDigits:2,
            maximumFractionDigits:2
        })+"x";

}

costInput.addEventListener("input",calculateMargin);
saleInput.addEventListener("input",calculateMargin);

document.getElementById("clearMargin").onclick=()=>{

    costInput.value="";
    saleInput.value="";

    calculateMargin();

};

/* INSTALAÇÃO */

let deferredPrompt=null;

window.addEventListener("beforeinstallprompt",event=>{

    event.preventDefault();
    deferredPrompt=event;

});

document.getElementById("installButton").onclick=async()=>{

    if(deferredPrompt){

        deferredPrompt.prompt();

        await deferredPrompt.userChoice;

        deferredPrompt=null;

        return;

    }

    if(/iPhone|iPad|iPod/i.test(navigator.userAgent)){

        alert(
            "Para instalar no iPhone:\n\n"+
            "1. Toque em Compartilhar.\n"+
            "2. Toque em Adicionar à Tela de Início.\n"+
            "3. Toque em Adicionar."
        );

    }else{

        alert(
            "No Android, abra pelo Google Chrome e toque em "+
            "Instalar aplicativo ou Adicionar à tela inicial."
        );

    }

};

/* OFFLINE */

if("serviceWorker" in navigator){

    window.addEventListener("load",()=>{

        navigator.serviceWorker.register("./sw.js")
        .catch(error=>console.log(error));

    });

}

render();
calculateMargin();

</script>

</body>
</html>

<!DOCTYPE html>
<html lang="pt-BR">
<head>

<meta charset="UTF-8">

<meta name="viewport"
content="width=device-width,initial-scale=1,maximum-scale=1,viewport-fit=cover">

<meta name="theme-color" content="#075b2c">

<meta name="apple-mobile-web-app-capable" content="yes">
<meta name="apple-mobile-web-app-title" content="Dialli">
<meta name="apple-mobile-web-app-status-bar-style" content="default">

<link rel="manifest" href="manifest.json">

<link rel="apple-touch-icon"
sizes="180x180"
href="icon-180.png">

<link rel="icon"
type="image/png"
sizes="192x192"
href="icon-192.png">

<title>Calculadora Dialli</title>

<style>

*{
box-sizing:border-box;
-webkit-tap-highlight-color:transparent;
}

html,body{
margin:0;
min-height:100%;
font-family:Arial,Helvetica,sans-serif;
background:#06351f;
}

body{
color:#fff;
}

.app{
min-height:100vh;
padding:8px 10px max(18px,env(safe-area-inset-bottom));
background:linear-gradient(145deg,#012b17,#00642f,#012616);
}

.box{
width:100%;
max-width:760px;
margin:auto;
}

.modebar{
display:flex;
justify-content:center;
gap:5px;
margin-bottom:5px;
}

.modebar button{
border:1px solid rgba(255,255,255,.35);
background:rgba(255,255,255,.1);
color:#fff;
border-radius:9px;
padding:6px 13px;
font-size:12px;
font-weight:bold;
}

.modebar button.active{
background:#ffd000;
color:#06351f;
}

.logo{
width:min(94%,520px);
margin:auto;
}

.logo svg{
width:100%;
display:block;
}

h1{
text-align:center;
font-size:29px;
margin:5px 0;
}

.intro{
text-align:center;
font-size:14px;
line-height:1.35;
margin:0 10px 13px;
}

.screen{
display:none;
}

.screen.active{
display:block;
}

.card,
.calc-card{
background:#fff;
border-radius:24px;
padding:15px;
border:3px solid #15914b;
}

.card label{
display:block;
color:#075b2c;
font-weight:bold;
font-size:15px;
margin-bottom:6px;
}

.input{
display:flex;
border:3px solid #15914b;
border-radius:13px;
overflow:hidden;
}

.input span{
display:flex;
align-items:center;
padding:0 13px;
color:#075b2c;
font-size:18px;
font-weight:bold;
}

.input input{
width:100%;
height:53px;
border:0;
outline:0;
font-size:20px;
padding:0 10px;
}

.hint{
font-size:12px;
color:#68766f;
margin:5px 0 15px;
}

.result{
background:#fff;
border-radius:20px;
padding:15px;
text-align:center;
margin-top:10px;
border:3px solid #38bf70;
}

.result h2{
color:#145d35;
font-size:17px;
margin:0;
}

.num{
font-size:31px;
font-weight:900;
color:#075b2c;
margin:8px 0 4px;
}

.result p{
color:#68756f;
font-size:13px;
margin:0;
}

.formulas{
background:#fff;
color:#123;
border-radius:20px;
padding:15px;
margin-top:10px;
font-size:13px;
line-height:1.55;
}

.formulas b{
color:#075b2c;
font-size:16px;
}

.action,
.install-button{
width:100%;
height:53px;
margin-top:12px;
border:0;
border-radius:14px;
font-weight:bold;
font-size:15px;
}

.action{
background:#075b2c;
color:#fff;
}

.install-button{
background:#15914b;
color:#fff;
}

.memory{
display:grid;
grid-template-columns:repeat(5,1fr);
gap:2px;
margin-bottom:4px;
}

.memory button{
border:0;
background:transparent;
color:#075b2c;
height:27px;
font-size:11px;
font-weight:bold;
}

.display{
background:#06351f;
border-radius:18px;
min-height:142px;
padding:12px 15px;
margin-bottom:5px;
display:flex;
flex-direction:column;
justify-content:flex-end;
text-align:right;
overflow:hidden;
}

.expression{
min-height:34px;
color:#aebbb4;
font-size:clamp(17px,4vw,25px);
white-space:nowrap;
overflow:hidden;
text-overflow:ellipsis;
}

.result-number{
color:#fff;
font-size:clamp(40px,11vw,60px);
font-weight:900;
line-height:1.05;
white-space:nowrap;
overflow:hidden;
text-overflow:ellipsis;
}

.keys{
display:grid;
grid-template-columns:repeat(4,1fr);
gap:2px;
}

.keys button{
height:62px;
border:0;
border-radius:50%;
background:transparent;
color:#075b2c;
font-size:25px;
font-weight:500;
display:flex;
align-items:center;
justify-content:center;
transition:transform .07s,background .07s;
}

.keys button:active{
background:rgba(21,145,75,.12);
transform:scale(.88);
}

.keys .op{
color:#15914b;
font-size:29px;
}

.keys .special{
font-size:22px;
}

.keys .clear{
color:#d85a54;
}

.keys .equal{
width:76px;
height:76px;
justify-self:center;
align-self:center;
background:#21c94b;
color:#fff;
font-size:34px;
font-weight:600;
}

.footer{
text-align:center;
padding:18px 0 4px;
font-size:14px;
line-height:1.5;
}

.footer strong{
display:block;
text-align:center;
font-size:20px;
}

@media(max-width:600px){

.keys button{
height:58px;
font-size:23px;
}

.keys .op{
font-size:27px;
}

.keys .equal{
width:70px;
height:70px;
font-size:31px;
}

}

</style>
</head>

<body>

<div class="app">
<div class="box">

<div class="modebar">

<button id="mTab" class="active">
MARGEM
</button>

<button id="cTab">
CALCULADORA
</button>

</div>

<div class="logo">

<svg viewBox="0 0 700 390">

<path
d="M75 185C110 80 270 30 440 55C555 70 630 120 650 190C610 105 485 80 370 88C240 95 135 130 75 185Z"
fill="#ffd000"/>

<path
d="M75 185C110 275 260 315 430 300C520 292 590 260 620 220C565 285 430 285 310 267C200 250 115 220 75 185Z"
fill="#08733f"/>

<text
x="350"
y="205"
text-anchor="middle"
font-family="Arial"
font-size="128"
font-weight="900"
font-style="italic"
fill="white">
Dialli
</text>

<text
x="350"
y="255"
text-anchor="middle"
font-family="Arial"
font-size="38"
fill="white">
Distribuidora
</text>

<text
x="350"
y="330"
text-anchor="middle"
font-family="Arial"
font-size="27"
font-style="italic"
fill="white">
“Distribuir para melhor servir”
</text>

</svg>

</div>

<section id="margin" class="screen active">

<h1>
CALCULADORA <span style="color:#ffd000">DIALLI</span>
</h1>

<p class="intro">
Digite o custo e o preço de venda.
A calculadora informa automaticamente lucro, margem e markup.
</p>

<div class="card">

<label>CUSTO DO PRODUTO</label>

<div class="input">

<span>R$</span>

<input id="cost"
inputmode="decimal"
placeholder="Digite o custo">

</div>

<div class="hint">Ex.: 6,90</div>

<label>PREÇO DE VENDA / OFERTA</label>

<div class="input">

<span>R$</span>

<input id="sale"
inputmode="decimal"
placeholder="Digite o preço">

</div>

<div class="hint">Ex.: 10,00</div>

</div>

<div class="result">
<h2>PREÇO DE VENDA</h2>
<div id="rv" class="num">—</div>
<p>Preço informado</p>
</div>

<div class="result">
<h2>LUCRO BRUTO</h2>
<div id="rl" class="num">—</div>
<p>Venda − custo</p>
</div>

<div class="result">
<h2>MARGEM REAL</h2>
<div id="rm" class="num">—</div>
<p>Lucro ÷ venda × 100</p>
</div>

<div class="result">
<h2>MARKUP</h2>
<div id="rk" class="num">—</div>
<p>Venda ÷ custo</p>
</div>

<div class="formulas">

<b>FÓRMULAS</b>

<br>

Lucro = Preço de Venda − Custo

<br>

Margem = Lucro ÷ Preço de Venda × 100

<br>

Markup = Preço de Venda ÷ Custo

</div>

<button id="clearMargin" class="action">
LIMPAR TUDO
</button>

</section>


<section id="calc" class="screen">

<h1>
CALCULADORA <span style="color:#ffd000">DIALLI</span>
</h1>

<p class="intro">
Calculadora completa para contas rápidas do dia a dia.
</p>

<div class="calc-card">

<div class="memory">

<button data-m="MC">MC</button>
<button data-m="MR">MR</button>
<button data-m="M+">M+</button>
<button data-m="M-">M−</button>
<button data-m="MS">MS</button>

</div>

<div class="display">

<div id="expr" class="expression"></div>

<div id="num" class="result-number">
0
</div>

</div>

<div class="keys">

<button class="special clear" data-a="clear">C</button>
<button class="special" data-a="paren">( )</button>
<button class="special" data-a="percent">%</button>
<button class="op" data-o="/">÷</button>

<button data-n="7">7</button>
<button data-n="8">8</button>
<button data-n="9">9</button>
<button class="op" data-o="*">×</button>

<button data-n="4">4</button>
<button data-n="5">5</button>
<button data-n="6">6</button>
<button class="op" data-o="-">−</button>

<button data-n="1">1</button>
<button data-n="2">2</button>
<button data-n="3">3</button>
<button class="op" data-o="+">+</button>

<button class="special" data-a="sign">+/−</button>
<button data-n="0">0</button>
<button class="special" data-a="decimal">,</button>
<button class="equal" data-a="equals">=</button>

</div>

</div>

</section>


<button id="install" class="install-button">
📲 INSTALAR CALCULADORA DIALLI
</button>


<div class="footer">

criado e desenvolvido por

<strong>
João Siena.
</strong>

</div>

</div>
</div>


<script>

/* =========================
   FORMATAÇÃO
========================= */

function parseBR(value){

value=String(value||"")
.trim()
.replace(/\s/g,"");

if(!value)return null;

if(value.includes(",") && value.includes(".")){

value=value.replace(/\./g,"").replace(",",".");
}

else if(value.includes(",")){

value=value.replace(",",".");
}

else if(
value.includes(".") &&
value.split(".")[1] &&
value.split(".")[1].length===3
){

value=value.replace(".","");
}

const n=Number(value);

return Number.isFinite(n)?n:null;

}


const nf=new Intl.NumberFormat(
"pt-BR",
{
useGrouping:true,
maximumFractionDigits:12
}
);


const money=new Intl.NumberFormat(
"pt-BR",
{
style:"currency",
currency:"BRL"
}
);


const percent=new Intl.NumberFormat(
"pt-BR",
{
minimumFractionDigits:2,
maximumFractionDigits:2
}
);


/* =========================
   MARGEM
========================= */

const cost=document.getElementById("cost");
const sale=document.getElementById("sale");

const rv=document.getElementById("rv");
const rl=document.getElementById("rl");
const rm=document.getElementById("rm");
const rk=document.getElementById("rk");


function marginCalc(){

const c=parseBR(cost.value);
const s=parseBR(sale.value);

if(c===null || s===null || c<=0){

rv.textContent="—";
rl.textContent="—";
rm.textContent="—";
rk.textContent="—";

return;

}

const lucro=s-c;

rv.textContent=money.format(s);
rl.textContent=money.format(lucro);
rm.textContent=percent.format((lucro/s)*100)+"%";
rk.textContent=percent.format(s/c)+"x";

}


cost.oninput=marginCalc;
sale.oninput=marginCalc;


document
.getElementById("clearMargin")
.onclick=function(){

cost.value="";
sale.value="";

marginCalc();

};


/* =========================
   ABAS
========================= */

const marginScreen=
document.getElementById("margin");

const calcScreen=
document.getElementById("calc");

const mTab=
document.getElementById("mTab");

const cTab=
document.getElementById("cTab");


mTab.onclick=function(){

marginScreen.classList.add("active");
calcScreen.classList.remove("active");

mTab.classList.add("active");
cTab.classList.remove("active");

};


cTab.onclick=function(){

calcScreen.classList.add("active");
marginScreen.classList.remove("active");

cTab.classList.add("active");
mTab.classList.remove("active");

};


/* =========================
   CALCULADORA
========================= */

let tokens=[];
let current="0";
let entering=true;
let memory=0;
let lastExpression="";


const expression=
document.getElementById("expr");

const numberDisplay=
document.getElementById("num");


const operators=["+","-","*","/"];

const symbols={
"+":"+",
"-":"−",
"*":"×",
"/":"÷"
};


function render(){

expression.textContent=
lastExpression ||
tokens.map(function(x){

if(operators.includes(x)){
return symbols[x];
}

return nf.format(Number(x));

}).join(" ");


if(current==="Erro"){

numberDisplay.textContent="Erro";
return;

}


numberDisplay.textContent=
nf.format(Number(current));

}


function number(n){

if(current==="Erro")clear();

if(entering){

current=n;
entering=false;
lastExpression="";

}else{

current=
current==="0"
?n
:current+n;

}

render();

}


function decimal(){

if(current==="Erro")clear();

if(entering){

current="0.";
entering=false;
lastExpression="";

}else if(!current.includes(".")){

current+=".";

}

render();

}


function operator(op){

if(current==="Erro")return;


if(!entering){

tokens.push(current);
entering=true;

}


if(
tokens.length &&
operators.includes(
tokens[tokens.length-1]
)
){

tokens[tokens.length-1]=op;

}else{

tokens.push(op);

}


lastExpression="";

render();

}


/* =========================
   CÁLCULO
========================= */

function calculate(list){

let values=[];
let ops=[];


function priority(op){

return op==="+"||op==="-"?1:2;

}


for(const token of list){

if(!operators.includes(token)){

values.push(Number(token));

continue;

}


while(
ops.length &&
priority(ops[ops.length-1])
>=priority(token)
){

const op=ops.pop();

const b=values.pop();
const a=values.pop();

if(a===undefined||b===undefined)
return null;

let result;

if(op==="+")result=a+b;

if(op==="-")result=a-b;

if(op==="*")result=a*b;

if(op==="/"){

if(b===0)return null;

result=a/b;

}

values.push(result);

}

ops.push(token);

}


while(ops.length){

const op=ops.pop();

const b=values.pop();
const a=values.pop();

if(a===undefined||b===undefined)
return null;

let result;

if(op==="+")result=a+b;
if(op==="-")result=a-b;
if(op==="*")result=a*b;

if(op==="/"){

if(b===0)return null;

result=a/b;

}

values.push(result);

}


if(values.length!==1)
return null;

return Math.round(
(values[0]+Number.EPSILON)*1e12
)/1e12;

}


/* =========================
   =
========================= */

function equals(){

let list=tokens.slice();


if(!entering){

list.push(current);

}


while(
operators.includes(
list[list.length-1]
)
){

list.pop();

}


if(!list.length)return;


const result=calculate(list);


lastExpression=
list.map(function(x){

if(operators.includes(x))
return symbols[x];

return nf.format(Number(x));

}).join(" ")+" =";


if(result===null){

current="Erro";
entering=true;

}else{

current=String(result);

tokens=[];

entering=true;

}


render();

}


/* =========================
   LIMPAR
========================= */

function clear(){

tokens=[];
current="0";
entering=true;
lastExpression="";

render();

}


/* =========================
   +/-
========================= */

function sign(){

if(current==="0"||current==="Erro")
return;

current=
current.startsWith("-")
?current.substring(1)
:"-"+current;

render();

}


/* =========================
   PORCENTAGEM
========================= */

function percentage(){

const v=Number(current);

if(!Number.isFinite(v))
return;

current=String(v/100);

render();

}


/* =========================
   BOTÕES
========================= */

document.querySelectorAll("[data-n]")
.forEach(function(button){

button.onclick=function(){

number(button.dataset.n);

};

});


document.querySelectorAll("[data-o]")
.forEach(function(button){

button.onclick=function(){

operator(button.dataset.o);

};

});


document.querySelectorAll("[data-a]")
.forEach(function(button){

button.onclick=function(){

const a=button.dataset.a;

if(a==="clear")clear();

if(a==="decimal")decimal();

if(a==="equals")equals();

if(a==="sign")sign();

if(a==="percent")percentage();

};

});


/* =========================
   MEMÓRIA
========================= */

document.querySelectorAll("[data-m]")
.forEach(function(button){

button.onclick=function(){

const a=button.dataset.m;
const v=Number(current);


if(a==="MC")memory=0;

if(a==="MR"){

current=String(memory);
entering=false;

}

if(a==="MS" && Number.isFinite(v))
memory=v;

if(a==="M+" && Number.isFinite(v))
memory+=v;

if(a==="M-" && Number.isFinite(v))
memory-=v;

render();

};

});


/* =========================
   TECLADO
========================= */

document.addEventListener(
"keydown",
function(e){

if(e.key>="0"&&e.key<="9")
number(e.key);

else if(e.key==="."||e.key===",")
decimal();

else if(operators.includes(e.key))
operator(e.key);

else if(e.key==="Enter"||e.key==="="){

e.preventDefault();
equals();

}

else if(e.key==="Escape")
clear();

}
);


/* ==================================================
   INSTALAÇÃO ANDROID / IOS
================================================== */

let deferredPrompt=null;


/*
   O Chrome envia este evento quando o site
   está realmente apto para instalação.
*/

window.addEventListener(
"beforeinstallprompt",
function(event){

event.preventDefault();

deferredPrompt=event;

}
);


const installButton=
document.getElementById("install");


installButton.addEventListener(
"click",
async function(){

/*
   ANDROID:
   se o Chrome disponibilizou o prompt,
   abrir imediatamente a janela nativa.
*/

if(deferredPrompt){

await deferredPrompt.prompt();

const result=
await deferredPrompt.userChoice;

console.log(
"Instalação:",
result.outcome
);

deferredPrompt=null;

return;

}


/*
   IPHONE:
   o iOS não disponibiliza o mesmo
   prompt automático do Android.
*/

const ios=
/iphone|ipad|ipod/i.test(
navigator.userAgent
);


if(ios){

alert(
"Para instalar no iPhone:\n\n"+
"Toque em Compartilhar → "+
"Adicionar à Tela de Início → "+
"Adicionar."
);

return;

}


/*
   Se o Chrome não liberou o prompt,
   significa que ainda não considera
   o site instalável naquele momento.
*/

alert(
"Abra esta página no Google Chrome "+
"e tente novamente."
);

});


/*
   Detecta instalação concluída.
*/

window.addEventListener(
"appinstalled",
function(){

deferredPrompt=null;

console.log(
"Calculadora Dialli instalada."
);

});


/* =========================
   SERVICE WORKER
========================= */

if("serviceWorker" in navigator){

window.addEventListener(
"load",
function(){

navigator.serviceWorker
.register("./sw.js")
.catch(function(error){

console.log(
"Erro no Service Worker:",
error
);

});

});

}


/* INICIA */

render();

</script>

</body>
</html>
